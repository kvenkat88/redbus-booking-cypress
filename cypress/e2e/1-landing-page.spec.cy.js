describe('Verify Environment Config', () => {
    it('Verify Environment', () => {
        // cy.vist() is defined in support/commands.js file
        cy.launchURL('/')
        // var isWin = Cypress.platform
        // cy.log(isWin)
    });
});
